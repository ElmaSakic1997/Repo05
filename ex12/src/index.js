function main() {
    // Only change code below this line
    var lasName = "Tesla";
    var lastLetterOfLastName = lasName[lasName.length - 1];
    // Only change code above this line
    return lastLetterOfLastName;
}
console.log(main());
module.exports = main;